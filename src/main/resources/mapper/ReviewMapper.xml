<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kosa.kojaping.mapper.ReviewMapper">
    <insert id="insertReview" >
        INSERT INTO review (accommodationNo, memberNo, rating, comment, createdAt)
        VALUES (#{accommodationNo}, #{memberNo}, #{rating}, #{comment}, now());
    </insert>
    <select id="selectReviewById" resultType="com.kosa.kojaping.entity.Review">
        select * from review
        where memberNo = #{memberNo} and accommodationNo = #{accommodationNo};
    </select>
</mapper>